Описание структуры проекта

Расположение модулей и их назначение
task4/
│
├── main.py  Главный модуль, консольный интерфейс пользователя
├── database.py Ядро БД: абстрактный класс и реализации (InMemory, JSON, CSV)
├── models.py Модели данных (Student)
├── errors.py Пользовательские исключения
├── validators.py Функции валидации данных
├── requirements.txt Зависимости для тестирования
├── README.md Документация проекта
│
├── data/ Папка для файлов БД (создаётся автоматически)
│ ├── db.json Файл для JSON БД
│ └── csv_db/ Папка для CSV БД
│
└── tests/ Автоматические тесты
├── init.py
├── test_file_json.py Тесты для JSON реализации
└── test_file_csv.py Тесты для CSV реализации



Назначение модулей

- main.py: Содержит класс `DatabaseCLI` — консольный интерфейс пользователя. При запуске предлагает выбрать тип БД (In-Memory, JSON, CSV). Обрабатывает ввод команд, вызывает методы БД, выводит результаты. Реализует управление текущей таблицей, парсинг аргументов, фильтров и команд сортировки.

- database.py: Содержит абстрактный класс `DatabaseInterface` и три реализации:
  - `InMemoryDatabase` — данные хранятся в оперативной памяти (теряются после выхода)
  - `FileDatabaseJSON` — данные сохраняются в JSON файл на диске
  - `FileDatabaseCSV` — данные сохраняются в CSV файлы (дополнительное задание)
  
  Каждая реализация поддерживает методы: `create_table`, `insert`, `get_by_id`, `get_all`, `filter`, `update`, `delete`, `sort`, `list_tables`, `drop_table`, `table_exists`.

- models.py: Определяет dataclass `Student` с полями: id, name, age, major, enrolled_at. Содержит методы `to_dict()` для сериализации и `from_dict()` для десериализации.

- errors.py: Кастомные исключения: `DatabaseError` (базовое), `RecordNotFoundError` (запись не найдена), `ValidationError` (ошибка валидации данных).

- validators.py: Функция `validate_student_data` для проверки имени, возраста, специальности.

- tests/: Модульные тесты для файловых реализаций БД (JSON и CSV).



Описание реализованной функциональности

Выбор типа базы данных

При запуске программа предлагает выбрать один из трёх режимов:

1. In-Memory — данные не сохраняются после выхода (для тестирования)
2. File (JSON) — данные сохраняются в JSON файл
3. File (CSV) — данные сохраняются в CSV файлы (дополнительное задание)



Управление таблицами

| Команда | Описание | Пример |
|---------|----------|--------|
| `tables` | Показать все таблицы в базе | `tables` |
| `use <name>` | Выбрать активную таблицу | `use students` |
| `create <name> <schema>` | Создать таблицу (схемы: students, books, employees) | `create my_students students` |
| `drop <name>` | Удалить таблицу (с подтверждением) | `drop my_students` |



CRUD операции (над текущей таблицей)

| Команда | Описание | Пример |
|---------|----------|--------|
| `add field=value...` | Добавить запись | `add name=Иван age=20 major=Физика email=ivan@test.com` |
| `list` | Показать все записи | `list` |
| `get <id>` | Показать запись по ID | `get 1` |
| `update <id> field=value...` | Обновить запись | `update 1 age=21 name=Петр` |
| `delete <id>` | Удалить запись | `delete 1` |



Фильтрация

| Оператор | Описание | Пример |
|----------|----------|--------|
| `field=value` | Точное совпадение | `filter name=Иван` |
| `field__gt=value` | Больше чем | `filter age__gt=20` |
| `field__lt=value` | Меньше чем | `filter age__lt=30` |
| `field__contains=value` | Содержит подстроку | `filter name__contains=ан` |
| `field__startswith=value` | Начинается с | `filter name__startswith=Ив` |



Сортировка

| Команда | Описание | Пример |
|---------|----------|--------|
| `sort <field>` | Сортировка по возрастанию | `sort age` |
| `sort <field> desc` | Сортировка по убыванию | `sort age desc` |

Особенности сортировки:
- Регистронезависимая сортировка строк
- None значения отправляются в конец
- Поддержка всех типов данных



Доступные схемы таблиц

| Схема | Поля |
|-------|------|
| `students` | name (str), age (int), major (str), email (str) |
| `books` | title (str), author (str), year (int), isbn (str) |
| `employees` | full_name (str), position (str), salary (float), department (str) |



Обработка ошибок

- `ValidationError` — неверные данные (пустые поля, неверный тип, возраст вне диапазона)
- `RecordNotFoundError` — запись не найдена
- `DatabaseError` — ошибки уровня базы данных (таблица не существует, файл повреждён)

Все ошибки перехватываются и выводятся понятным сообщением.




Различия между реализациями

| Характеристика | In-Memory | File (JSON) | File (CSV) |
|----------------|-----------|-------------|-------------|
| Сохранение данных | ❌ Теряются | ✅ Сохраняются | ✅ Сохраняются |
| Скорость работы | ⚡ Очень быстро | 🐢 Медленнее | 🐢 Медленнее |
| Читаемость файлов | - | Хорошая | Отличная (Excel) |
| Формат хранения | В памяти | JSON | CSV |
| Количество файлов | - | 1 файл на БД | 2 файла на таблицу |




Инструкция по запуску

Требования
- Python 3.10 или выше

Запуск приложения

1. Перейдите в директорию `task4`:
   
   cd task4

2. Запустите приложение:

python main.py

3. При запуске выберите тип базы данных:

ВЫБОР ТИПА БАЗЫ ДАННЫХ
==================================================
1. In-Memory (данные не сохраняются)
2. File (JSON) - данные сохраняются в JSON файл
3. File (CSV) - данные сохраняются в CSV файлы
==================================================
Выберите тип БД (1-3): 

4. После выбора БД автоматически создадутся демо-таблицы с тестовыми данными.